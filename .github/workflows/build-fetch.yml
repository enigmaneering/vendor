name: Build and Release fetch CLI

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.1)'
        required: true
        type: string

jobs:
  build-fetch:
    name: Build fetch CLI
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: macos-latest
            runner: macos-14
            platform: darwin-arm64
            goos: darwin
            goarch: arm64
          - os: macos-latest
            runner: macos-14
            platform: darwin-amd64
            goos: darwin
            goarch: amd64
          - os: ubuntu-latest
            runner: ubuntu-latest
            platform: linux-amd64
            goos: linux
            goarch: amd64
          - os: ubuntu-latest
            runner: ubuntu-24.04-arm
            platform: linux-arm64
            goos: linux
            goarch: arm64
          - os: windows-latest
            runner: windows-latest
            platform: windows-amd64
            goos: windows
            goarch: amd64
          - os: windows-latest
            runner: windows-latest
            platform: windows-arm64
            goos: windows
            goarch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: false

      - name: Build fetch (Unix)
        if: runner.os != 'Windows'
        run: |
          cd go/fetch
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o fetch-${{ matrix.platform }}

      - name: Build fetch (Windows)
        if: runner.os == 'Windows'
        run: |
          cd go/fetch
          $env:GOOS="${{ matrix.goos }}"
          $env:GOARCH="${{ matrix.goarch }}"
          go build -o fetch-${{ matrix.platform }}.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fetch-${{ matrix.platform }}
          path: |
            go/fetch/fetch-${{ matrix.platform }}
            go/fetch/fetch-${{ matrix.platform }}.exe

  create-release:
    name: Create fetch Release
    needs: [build-fetch]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Create and push tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          TAG="go/fetch/${{ inputs.version }}"
          git tag $TAG
          git push origin $TAG

      - name: Download fetch artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: 'fetch-*'

      - name: Display artifact structure
        run: ls -R artifacts

      - name: Create go/fetch Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: go/fetch/${{ inputs.version }}
          name: fetch cli ${{ inputs.version }}
          draft: false
          prerelease: false
          files: |
            artifacts/**/fetch-*
          body: |
            # go/fetch ${{ inputs.version }}

            CLI tool and Go module that downloads and extracts the enigmaneering redistributable binaries.

            By default, fetches the latest redistributables. View [all redistributable releases](https://git.enigmaneering.org/enigmaneering/redistributables/releases?q=redistributables) to see available versions.

            ## 🚀 Quick Start

            ### Using the fetch CLI

            Run the `fetch` binary to download the shader toolchain binaries:

            ```bash
            # macOS ARM64 (Apple Silicon)
            curl -L -o fetch https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-darwin-arm64
            chmod +x fetch
            ./fetch

            # macOS x86_64 (Intel)
            curl -L -o fetch https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-darwin-amd64
            chmod +x fetch
            ./fetch

            # Linux x86_64
            curl -L -o fetch https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-linux-amd64
            chmod +x fetch
            ./fetch

            # Linux ARM64
            curl -L -o fetch https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-linux-arm64
            chmod +x fetch
            ./fetch

            # Windows x86_64 (PowerShell)
            Invoke-WebRequest -Uri "https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-windows-amd64.exe" -OutFile "fetch.exe"
            .\fetch.exe

            # Windows ARM64 (PowerShell)
            Invoke-WebRequest -Uri "https://git.enigmaneering.org/enigmaneering/redistributables/releases/download/go/fetch/${{ inputs.version }}/fetch-windows-arm64.exe" -OutFile "fetch.exe"
            .\fetch.exe
            ```

            ### Using the Go Module

            ```go
            import "git.enigmaneering.org/enigmaneering/redistributables/go/fetch/gpu"

            func main() {
                // Downloads and extracts latest toolchain to ./external/
                if err := gpu.EnsureLibraries(); err != nil {
                    log.Fatal(err)
                }

                // Specify a version and custom directory
                os.Setenv("ENIGMATIC_GOFETCH_DIRECTORY", "./my-shaders")
                if err := gpu.EnsureLibrariesVersion("v0.0.45"); err != nil {
                    log.Fatal(err)
                }
            }
            ```

            Install:
            ```bash
            go get git.enigmaneering.org/enigmaneering/redistributables/go/fetch@go/fetch/${{ inputs.version }}
            ```

            ## 📦 Downloads

            Fetches the latest redistributables containing:

            - **glslang** - GLSL/ESSL → SPIRV compiler
            - **SPIRV-Cross** - SPIRV → GLSL/HLSL/MSL/WGSL transpiler
            - **DXC** - HLSL → SPIRV/DXIL compiler
            - **Naga** - WGSL/GLSL/SPIRV multi-format compiler

            ## 💻 Platforms

            - macOS ARM64, x86_64
            - Linux ARM64, x86_64
            - Windows ARM64, x86_64

            ## 🔄 Features

            - Automatic version detection and upgrades
            - Create `FREEZE` file to pin version
            - Version tracking via `.version` file
            - Custom directory via `ENIGMATIC_GOFETCH_DIRECTORY`

            ## 📝 CLI Usage

            ```bash
            fetch                     # Download latest
            fetch --version v0.0.45   # Download specific version
            fetch --dir ./shaders     # Custom output directory
            fetch --help              # Show help
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
